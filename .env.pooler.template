# UPDATED: Use Connection Pooler (Port 6543) for Supabase
# This bypasses firewall blocks on port 5432

# ⭐ STEP 1: Get this URL from Supabase
# Go to: https://supabase.com → Your Project → Settings → Database
# Copy the "Connection pooler" URL (looks like below)
DATABASE_URL_POOLER=postgresql://postgres.auneeeflawwqivzvdynw:[YOUR_PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require

# ⭐ STEP 2: Keep original URL as fallback (this will timeout but is here for reference)
DATABASE_URL=postgresql://postgres:Siddhu%402430@db.auneeeflawwqivzvdynw.supabase.co:5432/postgres

# Supabase URLs (no changes needed)
SUPABASE_URL=https://auneeeflawwqivzvdynw.supabase.co
SUPABASE_KEY=sb_publishable_naFmN8sh15fbku1R4dhW-g_voZmtflB

# Flask Secret
FLASK_SECRET_KEY=cvr_resolve_production_secret_2024_supabase_key

# Google OAuth (if using authentication)
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# ⭐ TO USE THIS:
# 1. Copy your password from Supabase (it's Siddhu%402430)
# 2. Edit DATABASE_URL_POOLER above - replace [YOUR_PASSWORD] with actual password
# 3. Save this as .env in cvr_resolve/ folder
# 4. Run: python -m flask run --host=0.0.0.0 --port=5000
#
# The app will now:
# ✅ Try Connection Pooler (port 6543) first
# ⚠️  Fall back to direct connection (port 5432) if pooler fails
# ✅ Store all data in Supabase
